Edit cimis.py to change end dates, to span simulation period

Run this script to generate union_city-hourly.nc

This netcdf file will be used by the startup scripts

You can plot it using plot_cimis.py

The climatology script is old, may need to be modified, if you need to fill in data